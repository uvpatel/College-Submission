#include <stdio.h>
#include <math.h>

int main(){
    int x;
    int eq = 3*(pow(x,3) + 2*pow(x,2) - 5*x + 1);
    printf("Enter the value of x: ");
    scanf("%d", &x);
    
    return 0;
}