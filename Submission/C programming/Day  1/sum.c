#include <stdio.h>

int main(){
    float a,b;
    float c = a + b;
    printf("Enter the value of a: ");
    scanf("%f",&a);
    printf("Enter the value of b: ");
    scanf("%f",&b);
    // show this programme give info that when we do any operation so first take input then assign any new varible.
    printf("The value of c is %f",c);

    float sum = a + b;

    
    printf("The value of sum is %f",sum);
  return 0;
}